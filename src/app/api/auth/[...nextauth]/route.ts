import { handlers, auth } from "@/auth"; import { NextResponse } from "next/server"; export async function GET(req: Request, context: any) { const params = await context.params; if (params.nextauth && params.nextauth[0] === "session") { const session = await auth(); return NextResponse.json(session || {}); } return (handlers.GET as any)(req, { params }); } export async function POST(req: Request, context: any) { const params = await context.params; return (handlers.POST as any)(req, { params }); }
